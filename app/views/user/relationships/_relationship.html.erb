<%= link_to "#{user.followings.count}人",follows_user_path(user.id),remote: true %>

<%= link_to "#{user.followers.count}人",followers_user_path(user.id),remote: true %><br>

<% if user.id != current_user.id %>
  <% if user.followed_by?(current_user) %>
    <%= link_to "フォロー中", user_relationships_path(user.id),method: :delete,remote: true %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user.id),method: :post,remote: true %>
    <% end %>
<% end %>