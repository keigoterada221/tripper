<!-- コメントのページネーション(使用する可能性があるので置いておく) -->
$("#comment").append('<%= j(render @comments) %>');
$("#view_more").replaceWith("<%= j(link_to_next_page(@comments, 'さらに読み込む', remote: true, id: 'view_more',class: 'btn btn-outline-light w-100 mt-3 mb-3')) %>");